spring:
  application:
    name: ciifm-config-service
  cloud:
    config:
      label: master
      profile: dev
      server:
        git:
          uri: https://github.com/ciifm/maven-repo-private
          username: ciifm
          password: Gm152688
          search-paths: config
server:
  port: 8888